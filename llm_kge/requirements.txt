# ============================================================
# Dependencias del pipeline KGE + LLM
# Crear entorno con Python 3.12 (PyKEEN no soporta Python 3.14):
#   python3.12 -m venv venv312
#   source venv312/bin/activate
#   pip install -r requirements.txt
# ============================================================

# --- Grafo RDF ---
rdflib==7.1.3

# --- PyTorch (base para KGE y LLM) ---
torch>=2.2.0

# --- KGE: TransE, DistMult, ComplEx con PyKEEN ---
pykeen>=1.10.0

# --- Extracción de entidades y relaciones (GLiNER2) ---
gliner2>=0.1.0

# --- LLM: HuggingFace Transformers ---
transformers>=4.40.0
accelerate>=0.29.0
sentencepiece>=0.2.0         # tokenizadores de modelos como Mistral/LLaMA

# --- Evaluación ---
bert-score>=0.3.13           # BERTScore semántico para respuestas en español
scikit-learn>=1.4.0          # utilidades métricas adicionales

# --- Utilidades ---
numpy>=1.26.0
pandas>=2.2.0
tqdm>=4.66.0

# --- Opcional: cuantización 4-bit para modelos 7B en GPU ---
# bitsandbytes>=0.43.1

vllm==0.18.0
